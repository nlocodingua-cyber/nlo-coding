/**
 * Aurora background — animated cyan + purple blobs behind hero sections.
 * Pure CSS (see .aurora in globals.css). No client runtime needed.
 */
export function AuroraBg() {
  return <div className="aurora" aria-hidden="true" />;
}
